"""
Unit tests for Confluence API Integration library.

These tests focus on testing individual components in isolation using mocks
to avoid dependencies on external services (Confluence API, Pandoc).
"""
